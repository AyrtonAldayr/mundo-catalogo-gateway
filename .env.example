# =============================================================================
# mundo-catalogo-gateway – Variables de entorno de ejemplo
# =============================================================================
# Copiar a .env y ajustar valores.
# - En local: muchos IDEs (VS Code con envFile, IntelliJ run configuration, etc.) cargan .env
#   al arrancar la app, así puedes tener perfil, puerto, OTLP, etc. sin configurarlos a mano.
# - En CI/CD: definir las mismas claves en el pipeline o en los secrets del orquestador.
# Spring Boot mapea UPPER_SNAKE_CASE a propiedades (ej. SERVER_PORT → server.port).
# No commitear .env (ya está en .gitignore).
# =============================================================================

# -----------------------------------------------------------------------------
# Perfil y Config Server
# -----------------------------------------------------------------------------
# Perfil activo: local | desarrollo | produccion (contenedor: local,local-container).
SPRING_PROFILES_ACTIVE=local

# URL del Config Server (opcional). Sin Config Server usar "optional:configserver:" o omitir.
# Ejemplo con Config Server: optional:configserver:http://config-server:8888
# En K8s suele venir del ConfigMap (ver k8s/configmap.yaml).
SPRING_CONFIG_IMPORT=optional:configserver:

# -----------------------------------------------------------------------------
# Servidor
# -----------------------------------------------------------------------------
# Puerto HTTP del gateway (por defecto 8080 en application.yaml).
SERVER_PORT=8080

# -----------------------------------------------------------------------------
# Gateway (configuración propia del proyecto)
# -----------------------------------------------------------------------------
# Habilitar validación JWT en paths configurados en gateway.security.secured-path-patterns.
# En local suele ser false; en desarrollo/producción true (o dejar que lo defina el perfil).
# GATEWAY_SECURITY_ENABLED=false

# -----------------------------------------------------------------------------
# Observabilidad (OpenTelemetry / OTLP)
# -----------------------------------------------------------------------------
# URLs del exportador OTLP. En local: localhost; en CI/producción: Collector o backend.
# Por defecto en application.yaml: localhost:4318. Sobrescribir en despliegue.
MANAGEMENT_OTLP_METRICS_EXPORT_URL=http://localhost:4318/v1/metrics
MANAGEMENT_OPENTELEMETRY_TRACING_EXPORT_OTLP_ENDPOINT=http://localhost:4318/v1/traces
MANAGEMENT_OPENTELEMETRY_LOGGING_EXPORT_OTLP_ENDPOINT=http://localhost:4318/v1/logs

# Ejemplo para producción (descomentar y ajustar host/puerto):
# MANAGEMENT_OTLP_METRICS_EXPORT_URL=http://otel-collector.monitoring.svc.cluster.local:4318/v1/metrics
# MANAGEMENT_OPENTELEMETRY_TRACING_EXPORT_OTLP_ENDPOINT=http://otel-collector.monitoring.svc.cluster.local:4318/v1/traces
# MANAGEMENT_OPENTELEMETRY_LOGGING_EXPORT_OTLP_ENDPOINT=http://otel-collector.monitoring.svc.cluster.local:4318/v1/logs

# -----------------------------------------------------------------------------
# Logging (logback-spring.xml)
# -----------------------------------------------------------------------------
# Ruta del directorio de logs y nombre del archivo (solo aplica en perfil local con archivo).
# En contenedor suele usarse solo consola; estos valores son opcionales.
# LOG_PATH=./logs
# LOG_FILE=./logs/mundo-catalogo-gateway.log

# -----------------------------------------------------------------------------
# Opcional (CI / contenedor)
# -----------------------------------------------------------------------------
# Opciones JVM (ej. límite de memoria en K8s). El deployment ya usa -XX:MaxRAMPercentage=75.0.
# JAVA_TOOL_OPTIONS=-XX:MaxRAMPercentage=75.0
